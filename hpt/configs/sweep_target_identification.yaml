program: sweep_train.py
method: random
command:
  - ${env}
  - ${interpreter}
  - ${program}
  - "train"
  - "-f"
  - "configs/claim_target_identification.jsonnet"
  - "-s"
  - "experiments/hpt_trial/target"
  - ${args}

metric:
  name: training_accuracy
  goal: maximize
parameters:

  trainer.optimizer.lr:
    values: [0.1, 0.05,0.03,0.01,0.005,0.003,0.001,0.0005,0.0003,0.0001,0.00003,0.00001,0.000003,0.000001]
  model.dropout:
    distribution: uniform
    min: 0.2
    max: 0.8
  model.encoder.dropout:
    distribution: uniform
    min: 0.2
    max: 0.8
  trainer.grad_norm:
    distribution: uniform
    min: 1
    max: 10
  trainer.patience:
    min: 3
    max: 7
  trainer.num_epochs:
    min: 4
    max: 15
  model.encoder.num_layers:
    min: 1
    max: 6
  model.encoder.hidden_size:
    min: 64
    max: 512